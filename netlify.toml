[build]
  command = "npm install"
  functions = "src"
  publish = "public" # Create an empty folder named 'public' in your root if it doesn't exist

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/api/:splat"
  status = 200